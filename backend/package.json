{
    "scripts": {
        "start": "ts-node src/server.ts",
        "build": "tsc",
        "install-deps": "npm install && cd ../frontend && npm install",
        "start-dev": "concurrently \"npm run start\" \"cd ../frontend && npm start\"",
        "start:prod": "node dist/server.js"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.1.0",
        "ollama": "^0.6.3"
    },
    "devDependencies": {
        "@types/cors": "^2.8.19",
        "@types/express": "^5.0.5",
        "@types/node": "^24.10.1",
        "ts-node": "^10.9.2",
        "typescript": "^5.9.3"
    }
}
